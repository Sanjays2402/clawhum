# Changelog

## 0.1.0

- Initial release.
- CLAP audio embeddings with hash fallback.
- FAISS HNSW + NumPy index backends.
- FastAPI service (`/match`, `/reindex`, `/feedback`).
- Next.js 15 web UI with recorder and waveform.
- CLI: index / match / serve / stats / feedback.
- Helm chart, Docker (cpu + cuda), terraform skeleton.
